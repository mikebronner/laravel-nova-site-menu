<a
    href="{{ $url }}"
    class="{{ $class }}"
    target="{{ $target }}"
    title="{{ $titleAttribute }}"
>
    {{ $navigationLabel }}
    <span class="block">
        {{ $description }}
    </span>
</a>
